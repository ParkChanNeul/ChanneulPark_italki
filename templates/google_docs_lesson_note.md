# Google Docs Lesson Note Template

> Copy this into the student's cumulative Google Doc after each lesson.
> Quizlet is for short review cards. This document is for visual structure, culture, and personalized notes.

---

## Lesson Info

| Field | Content |
|-------|---------|
| Student | `{student_name}` |
| Date | `{YYYY-MM-DD}` |
| Lesson | `Week {number}: {lesson_title}` |
| Goal | `{one practical goal the student can use in real life}` |
| Quizlet Review | `{quizlet_link}` |

---

## Today's Survival Goal

By the end of this lesson, you can say:

> `{main_korean_expression}`

Meaning:

> `{plain_english_meaning}`

Where to use it:

> `{real_life_situation}`

---

## Key Phrases

| Situation | Korean | Meaning | Sound |
|-----------|--------|---------|-------|
| `{situation_1}` | `{korean_1}` | `{english_1}` | `{sound_1}` |
| `{situation_2}` | `{korean_2}` | `{english_2}` | `{sound_2}` |
| `{situation_3}` | `{korean_3}` | `{english_3}` | `{sound_3}` |

---

## Visual Grammar Box

Use this section for the explanation that is too detailed for Quizlet.

Color code:

| Color | Role | Example |
|-------|------|---------|
| 🔵 Blue | Grammar tool | `{grammar_piece}` |
| 🟢 Green | Meaning unit | `{vocab_piece}` |
| 🟡 Yellow | Culture note | `{culture_piece}` |

Pattern:

```text
{short_pattern_name}

{example_korean}
= {example_english}

Use it when:
{simple_use_case}
```

Student-friendly explanation:

> `{one_or_two_sentences_only}`

---

## Culture Note

What sounds natural to Koreans:

> `{natural_korean_behavior}`

Why it works:

> `{short_culture_explanation}`

What to avoid:

> `{awkward_or_too_textbook_version}`

---

## Student Combinations

Record what the student created during class.

| Student tried | Natural version | Note |
|---------------|-----------------|------|
| `{student_sentence_1}` | `{natural_sentence_1}` | `{why_it_works}` |
| `{student_sentence_2}` | `{natural_sentence_2}` | `{why_it_works}` |

---

## Practice After Class

1. Review the Quizlet set: `{quizlet_link}`
2. Say these three lines out loud:
   - `{practice_line_1}`
   - `{practice_line_2}`
   - `{practice_line_3}`
3. Try one real-life use before the next class:

> `{tiny_real_life_mission}`

---

## Teacher Follow-Up

What improved:

- `{student_win_1}`
- `{student_win_2}`

Next lesson should repeat:

- `{repeat_item_1}`
- `{repeat_item_2}`

Next lesson should add:

- `{next_item_1}`
- `{next_item_2}`
